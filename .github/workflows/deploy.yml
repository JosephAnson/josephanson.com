name: Docker

on:
  push:
    branches:
      - master

  workflow_run:
    workflows: [CI]
    types:
      - completed

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - uses: actions/checkout@v4

      - name: Fetch secrets from Infisical
        uses: Infisical/secrets-action@v1.0.12
        with:
          method: "universal"
          client-id: ${{ secrets.INFISICAL_CLIENT_ID }}
          client-secret: ${{ secrets.INFISICAL_CLIENT_SECRET }}
          domain: "https://secrets.josephanson.com"
          project-slug: "josephanson"
          env-slug: "prod"

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/josephanson:latest
          build-args: |
            NUXT_PUBLIC_STUDIO_TOKENS=${{ env.NUXT_PUBLIC_STUDIO_TOKENS }}
            CHROME_TOKEN=${{ env.CHROME_TOKEN }}
            REDIS_HOST=${{ env.REDIS_HOST }}
            REDIS_PASSWORD=${{ env.REDIS_PASSWORD }}
            MINIO_URL=${{ env.MINIO_URL }}
            MINIO_ACCESS_KEY=${{ env.MINIO_ACCESS_KEY }}
            MINIO_SECRET_KEY=${{ env.MINIO_SECRET_KEY }}
